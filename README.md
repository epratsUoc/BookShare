# BookShare
Aplicaci√≥n para compartir libros con personas cercanas a ti desarrollada como pr√°ctica de un curso del SOC en colaboraci√≥n con laC

Se usa GitFlow por lo que todas las features branches se deben hacer a partir de la branch development.

Atenci√n! Cuando hag√is Git clone por defecto s√lo baja la master.
Deb√is bajar tambi√©n la development para hacer los git checkout -b featureX a partir de ah√≠.

Para hacerlo, si haceis git brach -a vereis el resto de branches. Basta con hacer git checkout development y tambi√n os bajar√°la branch adecuada.
